[project]
name = "local-mlx-stack"
version = "0.1.0"
description = "Version-controlled local MLX inference stack for Apple Silicon"
requires-python = ">=3.11,<3.13"
dependencies = [
    "mlx-vlm==0.4.4",
    "torchvision==0.26.0",
    "huggingface_hub==1.13.0",
]
