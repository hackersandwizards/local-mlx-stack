[project]
name = "local-mlx-stack"
version = "0.2.0"
description = "Local inference stack for Apple Silicon. oMLX server + Qwen3.6-27B."
requires-python = ">=3.11,<3.13"
dependencies = [
    "huggingface_hub==1.13.0",
]
